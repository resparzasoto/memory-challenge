'use strict';

const BLINK = 300;
const MAX_TIME = 1500;
const START_LEVEL = 1;
const SUBLEVEL_TO_LEVEL_UP = 2;
const GAME_OVER = 3;
const MIN_WORD_LENGTH = 5;
const MAX_WORD_LENGTH = 12;

module.exports = {
  BLINK,
  MAX_TIME,
  START_LEVEL,
  SUBLEVEL_TO_LEVEL_UP,
  GAME_OVER,
  MIN_WORD_LENGTH,
  MAX_WORD_LENGTH,
};
